import dotenv from "dotenv";
import mongoose from "mongoose";
import { Schema, model } from "mongoose";
import { app, router } from "./express.js";
export { dotenv, app, router, mongoose, Schema, model };
